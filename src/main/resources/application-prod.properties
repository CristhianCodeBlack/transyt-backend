# Configuración específica para producción
server.port=${PORT:8080}

# Base de datos PostgreSQL
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:backup}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:transyt123}

# JPA optimizado para producción
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT
jwt.secret=${JWT_SECRET:mySecretKey123456789012345678901234567890}
jwt.expiration=86400000

# Cloudinary
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:djlyguxbh}
cloudinary.api-key=${CLOUDINARY_API_KEY:133653877283912}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:Ucs7SOHT2OMfJz_rSIW2iFLpNA4}
cloudinary.secure=true

# CORS
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://transyt-frontend.onrender.com}

# Archivos
spring.servlet.multipart.max-file-size=500MB
spring.servlet.multipart.max-request-size=500MB
file.upload.base-url=${FILE_BASE_URL:https://transyt-backend.onrender.com}

# Logging mínimo
logging.level.org.springframework.security=WARN
logging.level.com.capacitapro.backend=INFO
logging.level.root=WARN